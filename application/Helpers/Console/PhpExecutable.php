<?php

namespace Navel\Helpers\Console;

class PhpExecutable
{
    public function find( $command )
    {
        // check if $command is array or not

        // Check if exec($command) exists

        // return exec($command);
    }
}
