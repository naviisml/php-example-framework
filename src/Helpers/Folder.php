<?php

namespace Navel\Helpers;

class Folder
{
    /**
     * Return the files in a specific folder
     *
     * @param  string $folder
     * @return array $path, $folders, $files
     */
    public function open( $folder )
    {
        // return [ 'path' => $folder, 'folders' => $folders ?? null, 'files' => $files ?? null ];
    }
}
